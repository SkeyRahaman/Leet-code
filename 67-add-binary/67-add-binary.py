class Solution:
    def addBinary(self, a: str, b: str) -> str:
        # a,b = a[::-1], b[::-1]
        # carry = 0
        # output = []
        # i = 0
        # for i,j in zip(a,b):
        #     if a == b == 1:
        #         output.append("0")
        #     elif a == b == 
        return bin(int(a,2) + int(b,2))[2:]
            
            
        
        